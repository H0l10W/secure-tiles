{
  "name": "secure-tiles-cloudflare-relay",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --test --test-isolation=none worker.test.js",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.123.0"
  }
}
